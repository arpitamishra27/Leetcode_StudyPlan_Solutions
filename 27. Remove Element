class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:

        count=0
        for j in range(len(nums)):
          if nums[j]!=val:
              nums[count]=nums[j]
              count+=1
        return count 
